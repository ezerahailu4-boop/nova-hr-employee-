import { NextApiRequest, NextApiResponse } from "next"; import { createClient } from "@supabase/supabase-js"; import { verifyAdminToken } from "../../../lib/auth"; const supabase = createClient(process.env.SUPABASE_URL || "https://aunkcnmplnunnercrvni.supabase.co", process.env.SUPABASE_SERVICE_KEY || process.env.SUPABASE_KEY || ""); const BUCKET = process.env.SUPABASE_BUCKET || "cvs"; export default async function handler(req: NextApiRequest, res: NextApiResponse) { const token = req.headers.authorization?.replace("Bearer ", "") || ""; const isValid = await verifyAdminToken(token); if (!isValid) return res.status(401).json({ success: false, error: "Authentication required" }); const { subId } = req.query; if (!subId || typeof subId !== "string") return res.status(400).json({ success: false, error: "Invalid ID" }); if (req.method === "GET") { const { data, error } = await supabase.from("submissions").select("*").eq("id", subId).single(); if (error || !data) return res.status(404).json({ success: false, error: "Not found" }); if (req.query.action === "cv_url" && data.cv_path) { try { const filename = data.cv_path.includes("/" + BUCKET + "/") ? data.cv_path.split("/" + BUCKET + "/").pop() : data.cv_path.split("/").pop(); const { data: signed, error: sErr } = await supabase.storage.from(BUCKET).createSignedUrl(filename, 3600); if (sErr) throw sErr; return res.status(200).json({ success: true, cv_url: signed.signedUrl }); } catch (e: any) { return res.status(200).json({ success: true, cv_url: data.cv_path, warning: e.message }); } } return res.status(200).json({ success: true, submission: data }); } if (req.method === "PATCH") { const { status } = req.body; if (!["pending","accepted","rejected"].includes(status)) return res.status(400).json({ success: false, error: "Invalid status" }); const { error } = await supabase.from("submissions").update({ status, updated_at: new Date().toISOString() }).eq("id", subId); if (error) return res.status(500).json({ success: false, error: error.message }); return res.status(200).json({ success: true, status }); } return res.status(405).json({ success: false, error: "Method not allowed" }); }